<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/vinovault.css" />
    <title>VinoVault</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>VinoVault</h1>
    <a href="/users/<%= user._id %>/vinos/new">Add a New Wine</a>
    <section class="hero">
      <ul>
        <% wines.forEach((wine)=>{ %> 
          <a href="/users/<%= user._id %>/vinos/<%= wine._id %>"><li><%= wine.name %> <%= wine.year %></a>, 
            <%= wine.winery %>, <%= wine.category %>
            <% }) %>
          </ul>
        </section>
      </body>
      </html>